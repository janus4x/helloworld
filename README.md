# 🚀 Hello World CI/CD Project

Креативный проект для тестирования CI/CD стека: **Node.js + Docker + MongoDB**

## 📋 Описание

Этот проект представляет собой веб-приложение с красивым дашбордом для мониторинга состояния системы, подключения к MongoDB и статистики использования. Проект полностью настроен для работы с Docker и CI/CD пайплайнами.

## ✨ Возможности

- 📊 **Мониторинг системы** - информация о Node.js, памяти, времени работы
- 🍃 **MongoDB интеграция** - статус подключения, статистика базы данных
- 📈 **Статистика приложения** - подсчет запросов, визитов, API вызовов
- 🏥 **Health Check** - проверка работоспособности всех компонентов
- 👥 **История визитов** - отслеживание посещений сайта
- 🎨 **Современный UI** - красивый интерфейс с градиентами и анимациями

## 🛠 Технологии

- **Node.js** 18+ с Express
- **MongoDB** 7
- **Docker** & Docker Compose
- **GitHub Actions** для CI/CD

## 📦 Установка и запуск

### Вариант 1: С Docker Compose (рекомендуется)

```bash
# Клонируйте репозиторий
git clone <your-repo-url>
cd helloworld

# Запустите все сервисы одной командой
docker-compose up -d

# Приложение будет доступно по адресу
# http://localhost:3000
```

### Вариант 2: Локальная установка

```bash
# Установите зависимости
npm install

# Убедитесь, что MongoDB запущена локально
# Или используйте Docker только для MongoDB:
docker run -d -p 27017:27017 --name mongodb mongo:7

# Создайте .env файл (опционально)
cp .env.example .env

# Запустите приложение
npm start
```

## 🐳 Docker команды

```bash
# Сборка образа
docker build -t helloworld-app .

# Запуск с docker-compose
docker-compose up -d

# Просмотр логов
docker-compose logs -f app

# Остановка
docker-compose down

# Остановка с удалением volumes
docker-compose down -v
```

## 🔌 API Endpoints

- `GET /` - Главная страница с дашбордом
- `GET /api/health` - Health check endpoint
- `GET /api/system` - Информация о системе
- `GET /api/mongodb` - Статус MongoDB
- `GET /api/stats` - Статистика приложения

## 🔄 CI/CD Pipeline

Проект включает GitHub Actions workflow (`.github/workflows/ci-cd.yml`), который:

1. ✅ **Тестирует** приложение с MongoDB
2. 🏗️ **Собирает** Docker образ
3. 🚀 **Готов к деплою** (можно добавить свои шаги)

### Запуск CI/CD

Просто запушьте код в репозиторий:

```bash
git add .
git commit -m "Initial commit"
git push origin main
```

GitHub Actions автоматически запустит пайплайн.

## ☁️ Развертывание в Coolify

Проект полностью готов для развертывания в Coolify! 

📖 **Подробная инструкция**: см. файл [COOLIFY.md](./COOLIFY.md)

### Быстрый старт в Coolify:

1. Подключите Git репозиторий в Coolify
2. Установите переменные окружения:
   - `MONGODB_URI=mongodb://mongodb:27017/helloworld` (или ваш URI)
   - `NODE_ENV=production`
   - `PORT=3000`
3. Разверните MongoDB как отдельный сервис в Coolify
4. Готово! 🎉

## 📁 Структура проекта

```
helloworld/
├── .github/
│   └── workflows/
│       └── ci-cd.yml          # CI/CD конфигурация
├── public/
│   └── index.html              # Главная страница
├── .dockerignore
├── .gitignore
├── docker-compose.yml          # Docker Compose для локальной разработки (с MongoDB)
├── docker-compose.yaml         # Docker Compose для Coolify (только приложение)
├── docker-compose.coolify.yml  # Альтернативная конфигурация для Coolify
├── Dockerfile                  # Docker образ приложения
├── COOLIFY.md                  # Инструкция по развертыванию в Coolify
├── package.json
├── server.js                   # Основной сервер
└── README.md
```

## 🔧 Переменные окружения

- `PORT` - Порт приложения (по умолчанию: 3000)
- `NODE_ENV` - Окружение (development/production)
- `MONGODB_URI` - URI подключения к MongoDB

## 🎯 Использование для тестирования CI/CD

Этот проект идеально подходит для:

- Тестирования Docker сборок
- Настройки CI/CD пайплайнов
- Изучения работы с MongoDB в контейнерах
- Демонстрации DevOps практик
- Тестирования деплоя на различные платформы

## 📝 Лицензия

MIT

## 🤝 Вклад

Приветствуются любые улучшения и предложения!

---

**Создано с ❤️ для тестирования CI/CD**

