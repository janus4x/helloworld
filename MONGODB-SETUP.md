# 🍃 Настройка MongoDB в Coolify

Инструкция по развертыванию MongoDB как отдельного сервиса в Coolify для проекта Hello World CI/CD.

## 📋 Важно!

**MongoDB должна быть развернута как ОТДЕЛЬНЫЙ сервис** в Coolify, а не как часть docker-compose приложения.

## 🚀 Способ 1: MongoDB как отдельный сервис в Coolify (рекомендуется)

### Шаг 1: Создание MongoDB сервиса

1. В Coolify перейдите в ваш проект
2. Нажмите **"Create New Resource"** → **"Database"** или **"Service"**
3. Выберите тип: **MongoDB**

### Шаг 2: Настройка MongoDB

**Основные настройки:**
- **Image:** `mongo:7` (или последняя версия)
- **Name:** `mongodb` (важно! это имя будет использоваться в MONGODB_URI)
- **Port:** `27017` (внутренний порт)
- **Volume:** Создайте volume для данных (например, `mongodb_data`)

**Переменные окружения (опционально):**
```env
MONGO_INITDB_DATABASE=helloworld
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=your_secure_password
```

**Если используете аутентификацию:**
```env
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=your_secure_password
```

### Шаг 3: Получение Connection String

После развертывания MongoDB сервиса:

1. **Без аутентификации:**
   ```
   mongodb://mongodb:27017/helloworld
   ```

2. **С аутентификацией:**
   ```
   mongodb://admin:your_secure_password@mongodb:27017/helloworld?authSource=admin
   ```

3. **Имя сервиса** (`mongodb`) - это имя, которое вы указали при создании сервиса в Coolify

### Шаг 4: Настройка приложения

В вашем приложении (Hello World) установите переменную окружения:

```env
MONGODB_URI=mongodb://mongodb:27017/helloworld
```

**Важно:** 
- Используйте имя сервиса MongoDB (`mongodb`) вместо `localhost`
- Coolify автоматически создаст внутреннюю сеть между сервисами
- Порт `27017` - это внутренний порт MongoDB контейнера

## 🌐 Способ 2: Внешний MongoDB сервер

Если у вас уже есть MongoDB на внешнем сервере:

### Настройка переменных окружения:

```env
MONGODB_URI=mongodb://user:password@your-mongodb-server.com:27017/helloworld?authSource=admin
```

Или для MongoDB Atlas:
```env
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/helloworld?retryWrites=true&w=majority
```

## 🔍 Проверка подключения

После настройки проверьте:

1. **Логи приложения** в Coolify:
   ```
   ✅ MongoDB подключена успешно
   ```

2. **Health Check endpoint:**
   ```
   https://your-app.coolify.io/api/health
   ```
   Должен показать `"database": "ok"`

3. **MongoDB статус:**
   ```
   https://your-app.coolify.io/api/mongodb
   ```
   Должен показать информацию о подключении

## 🐛 Решение проблем

### Проблема: "MongoDB не подключается"

**Возможные причины и решения:**

1. **Неправильное имя сервиса**
   - Убедитесь, что имя MongoDB сервиса в Coolify совпадает с именем в `MONGODB_URI`
   - Проверьте имя сервиса в Coolify UI

2. **MongoDB еще не запустилась**
   - Подождите несколько секунд после развертывания
   - Проверьте логи MongoDB сервиса в Coolify

3. **Неправильный порт**
   - Внутри Coolify используйте порт `27017` (внутренний)
   - Не используйте внешний порт

4. **Проблемы с сетью**
   - Убедитесь, что оба сервиса в одном проекте Coolify
   - Coolify автоматически создает сеть между сервисами

5. **Аутентификация**
   - Если MongoDB требует аутентификацию, добавьте credentials в URI
   - Формат: `mongodb://username:password@mongodb:27017/helloworld?authSource=admin`

### Проблема: "Connection timeout"

**Решение:**
- Проверьте, что MongoDB сервис запущен и здоров
- Убедитесь, что имя сервиса правильное
- Проверьте логи MongoDB на ошибки

## 📝 Пример полной настройки

### 1. MongoDB сервис в Coolify:

**Настройки:**
- Name: `mongodb`
- Image: `mongo:7`
- Port: `27017`

**Переменные окружения:**
```env
MONGO_INITDB_DATABASE=helloworld
```

### 2. Приложение Hello World:

**Переменные окружения:**
```env
NODE_ENV=production
PORT=3000
MONGODB_URI=mongodb://mongodb:27017/helloworld
```

### 3. Результат:

- MongoDB доступна по адресу `mongodb:27017` из приложения
- Приложение автоматически подключается к MongoDB
- Данные сохраняются в volume MongoDB

## 🔐 Безопасность

### Для production:

1. **Используйте аутентификацию:**
   ```env
   MONGO_INITDB_ROOT_USERNAME=admin
   MONGO_INITDB_ROOT_PASSWORD=strong_password_here
   ```

2. **Обновите MONGODB_URI:**
   ```env
   MONGODB_URI=mongodb://admin:strong_password_here@mongodb:27017/helloworld?authSource=admin
   ```

3. **Не храните пароли в коде** - используйте переменные окружения Coolify

## 📚 Дополнительные ресурсы

- [MongoDB Docker Hub](https://hub.docker.com/_/mongo)
- [MongoDB Connection Strings](https://www.mongodb.com/docs/manual/reference/connection-string/)
- [Coolify Database Services](https://coolify.io/docs)

---

**Готово!** MongoDB настроена и готова к работе! 🎉

