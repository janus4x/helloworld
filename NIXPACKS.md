# 🚀 Развертывание с Nixpacks

Инструкция по развертыванию проекта Hello World CI/CD с использованием **Nixpacks Build Pack** в Coolify.

## 📋 Что такое Nixpacks?

Nixpacks - это современный инструмент для автоматической сборки Docker образов, который:
- ✅ Автоматически определяет тип проекта (Node.js, Python, Ruby и т.д.)
- ✅ Устанавливает зависимости без необходимости Dockerfile
- ✅ Оптимизирует размер образа
- ✅ Поддерживает множество языков и фреймворков

## 🎯 Преимущества Nixpacks

1. **Простота** - не нужен Dockerfile
2. **Автоматизация** - Nixpacks сам определяет зависимости
3. **Оптимизация** - автоматическая оптимизация слоев
4. **Гибкость** - легко переключаться между версиями Node.js

## 📦 Настройка проекта для Nixpacks

Проект уже настроен для работы с Nixpacks:

### Файлы конфигурации:

1. **`nixpacks.toml`** - основная конфигурация Nixpacks
2. **`.nvmrc`** - указывает версию Node.js (18)
3. **`package.json`** - содержит `engines` для указания версии Node.js

### Версия Node.js

Версия Node.js определяется в следующем порядке приоритета:

1. **Переменная окружения** `NIXPACKS_NODE_VERSION` (в Coolify UI)
2. **Файл `.nvmrc`** (содержит `18`)
3. **Поле `engines` в `package.json`** (указано `>=18.0.0`)
4. **По умолчанию** - Node.js 18

## 🔧 Развертывание в Coolify с Nixpacks

### Шаг 1: Подключение репозитория

1. В Coolify создайте новый ресурс
2. Выберите "Public Repository" или подключите через GitHub App
3. Укажите URL вашего репозитория

### Шаг 2: Выбор Build Pack

1. В настройках ресурса выберите **"Nixpacks"** как Build Pack
2. Coolify автоматически определит проект как Node.js

### Шаг 3: Настройка Build Pack

В настройках Build Pack укажите:

- **Base Directory:** `/` (корень проекта)
- **Port:** `3000` (или используйте переменную `PORT`)
- **Is it a static Site?:** ❌ Нет (это динамическое приложение)
- **Build Command:** (оставьте пустым, Nixpacks определит автоматически)
- **Start Command:** `node server.js` (или оставьте пустым, используется из package.json)

### Шаг 4: Развертывание MongoDB (ОБЯЗАТЕЛЬНО!) ⚠️

**⚠️ КРИТИЧЕСКИ ВАЖНО: MongoDB должна быть развернута как ОТДЕЛЬНЫЙ сервис в Coolify!**

**Nixpacks собирает ТОЛЬКО ваше Node.js приложение.** MongoDB **НЕ включается** в сборку автоматически. 

Вы **ОБЯЗАТЕЛЬНО** должны создать **отдельный MongoDB сервис** в Coolify перед развертыванием приложения!

#### 🚀 Пошаговая инструкция:

1. **Создайте новый ресурс в Coolify** (отдельно от вашего приложения)
   - Нажмите "Create New Resource" или "New"
   - Выберите тип: **"Database"** → **"MongoDB"** (если доступно)
   - ИЛИ выберите **"Docker Image"**

2. **Настройте MongoDB сервис:**
   
   **Если используете Database → MongoDB:**
   - Имя сервиса: `mongodb` ⚠️ **ОБЯЗАТЕЛЬНО запомните это имя!**
   - Версия: `7` или `mongo:7`
   - Port: `27017`
   
   **Если используете Docker Image:**
   - Image: `mongo:7`
   - Имя сервиса: `mongodb` ⚠️ **ОБЯЗАТЕЛЬНО запомните это имя!**
   - Port: `27017`
   - Environment Variables:
     ```env
     MONGO_INITDB_DATABASE=helloworld
     ```

3. **Нажмите Deploy** и дождитесь запуска MongoDB

4. **Проверьте, что MongoDB запущена:**
   - В логах должно быть: `Waiting for connections`
   - Статус сервиса должен быть "Running"

5. **Запомните имя сервиса** - оно понадобится для `MONGODB_URI` в следующем шаге!

📖 **Подробная инструкция со всеми вариантами:** см. [MONGODB-SETUP.md](./MONGODB-SETUP.md)

### Шаг 5: Переменные окружения

**⚠️ СНАЧАЛА УБЕДИТЕСЬ, ЧТО MONGODB УЖЕ РАЗВЕРНУТА!** (см. Шаг 4)

Установите следующие переменные окружения в Coolify для вашего **Node.js приложения**:

```env
NODE_ENV=production
PORT=3000
MONGODB_URI=mongodb://mongodb:27017/helloworld
```

**⚠️ КРИТИЧЕСКИ ВАЖНО про MONGODB_URI:** 

- `mongodb` в URI - это **имя вашего MongoDB сервиса** в Coolify (то, что вы указали при создании в Шаге 4)
- Если вы назвали сервис по-другому (например, `my-mongodb`), **обязательно замените** `mongodb` на ваше имя:
  ```env
  MONGODB_URI=mongodb://my-mongodb:27017/helloworld
  ```
- Coolify автоматически создает внутреннюю сеть, поэтому сервисы могут обращаться друг к другу по имени
- Если MongoDB внешняя, укажите полный URI: `mongodb://user:pass@host:27017/helloworld`
- Для MongoDB Atlas используйте: `mongodb+srv://user:pass@cluster.mongodb.net/helloworld`

**Как узнать имя сервиса MongoDB:**
- Откройте ваш MongoDB сервис в Coolify
- Посмотрите на название ресурса вверху страницы
- Это и есть имя, которое нужно использовать в `MONGODB_URI`

📖 **Подробная инструкция по настройке MongoDB:** см. [MONGODB-SETUP.md](./MONGODB-SETUP.md)

### Шаг 5: Развертывание

1. Нажмите "Deploy"
2. Nixpacks автоматически:
   - Определит Node.js проект
   - Установит Node.js 18 (из .nvmrc)
   - Установит зависимости через `npm install`
   - Соберет оптимизированный образ
   - Запустит приложение

## 🔍 Проверка развертывания

После развертывания проверьте:

1. **Логи** в Coolify - должны быть сообщения:
   ```
   ✅ MongoDB подключена успешно
   🚀 Сервер запущен на порту 3000
   ```

2. **Health Check:** `https://your-app.coolify.io/api/health`
   - Должен вернуть JSON с статусом

3. **Главная страница:** `https://your-app.coolify.io/`
   - Должен отображаться дашборд

## ⚙️ Дополнительная настройка

### Изменение версии Node.js

**Способ 1: Через переменную окружения**
```env
NIXPACKS_NODE_VERSION=20
```

**Способ 2: Обновить `.nvmrc`**
```
20
```

**Способ 3: Обновить `package.json`**
```json
{
  "engines": {
    "node": ">=20.0.0"
  }
}
```

### Кастомная конфигурация Nixpacks

Отредактируйте `nixpacks.toml` для дополнительных настроек:

```toml
[phases.setup]
# Кастомные команды установки

[phases.install]
# Кастомные команды установки зависимостей

[start]
cmd = "node server.js"
```

## 🆚 Nixpacks vs Dockerfile

### Когда использовать Nixpacks:
- ✅ Простые проекты без сложной сборки
- ✅ Хотите автоматическую оптимизацию
- ✅ Быстрое развертывание без написания Dockerfile
- ✅ Стандартные Node.js приложения

### Когда использовать Dockerfile:
- ✅ Сложная кастомная сборка
- ✅ Нужен полный контроль над процессом
- ✅ Специфичные системные зависимости
- ✅ Мультистейдж сборка

## 🐛 Решение проблем

### Проблема: Неправильная версия Node.js

**Решение:**
1. Проверьте `.nvmrc` файл
2. Установите переменную `NIXPACKS_NODE_VERSION` в Coolify
3. Обновите `engines` в `package.json`

### Проблема: Зависимости не устанавливаются

**Решение:**
1. Убедитесь, что `package.json` корректен
2. Проверьте логи сборки в Coolify
3. Убедитесь, что `package-lock.json` закоммичен

### Проблема: Приложение не запускается

**Решение:**
1. Проверьте команду запуска в `package.json` (`npm start`)
2. Убедитесь, что порт указан правильно
3. Проверьте переменные окружения

## 📚 Дополнительные ресурсы

- [Документация Nixpacks](https://nixpacks.com/docs)
- [Nixpacks для Node.js](https://nixpacks.com/docs/providers/node)
- [Coolify Nixpacks Guide](https://coolify.io/docs/applications/build-packs/nixpacks)

## 🎉 Готово!

Ваше приложение теперь развернуто с Nixpacks! 

**Преимущества:**
- ✅ Автоматическая сборка без Dockerfile
- ✅ Оптимизированный образ
- ✅ Простое управление версиями Node.js
- ✅ Быстрое развертывание

---

**Совет:** Если нужен больший контроль, можно переключиться на Dockerfile в настройках Coolify в любой момент.

