# 🚀 Развертывание в Coolify

Инструкция по развертыванию проекта Hello World CI/CD в Coolify.

## 📋 Подготовка

### Вариант 1: Развертывание через Git репозиторий (рекомендуется)

1. **Подключите репозиторий** в Coolify
2. Coolify автоматически определит `Dockerfile`
3. Настройте переменные окружения (см. ниже)

### Вариант 2: Развертывание через Docker Compose

1. Используйте файл `docker-compose.coolify.yml`
2. Или создайте новый сервис в Coolify и укажите путь к `Dockerfile`

## 🔧 Настройка переменных окружения

В Coolify перейдите в настройки приложения → Environment Variables и добавьте:

### Обязательные переменные:

```env
NODE_ENV=production
PORT=3000
MONGODB_URI=mongodb://<mongodb-service-name>:27017/helloworld
```

### Примеры MONGODB_URI:

**Если MongoDB развернута в Coolify как отдельный сервис:**
```env
MONGODB_URI=mongodb://mongodb:27017/helloworld
```

**Если MongoDB на внешнем сервере:**
```env
MONGODB_URI=mongodb://user:password@mongodb.example.com:27017/helloworld?authSource=admin
```

**Если MongoDB в MongoDB Atlas:**
```env
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/helloworld?retryWrites=true&w=majority
```

## 🍃 Настройка MongoDB в Coolify

### Способ 1: MongoDB как отдельный сервис в Coolify

1. Создайте новый сервис в Coolify
2. Выберите образ: `mongo:7`
3. Добавьте переменные окружения:
   ```env
   MONGO_INITDB_DATABASE=helloworld
   ```
4. Запомните имя сервиса (например, `mongodb`)
5. В приложении используйте `MONGODB_URI=mongodb://mongodb:27017/helloworld`

### Способ 2: Внешняя MongoDB

Используйте внешний MongoDB сервер или MongoDB Atlas и укажите полный URI в переменных окружения.

## ⚙️ Настройки приложения в Coolify

### Build Settings (если используете Dockerfile):

- **Dockerfile Path**: `Dockerfile` (по умолчанию)
- **Build Context**: `.` (корень проекта)

### Port Settings:

- **Port**: `3000` (или используйте переменную `PORT`)
- Coolify автоматически пробросит порт

### Health Check:

Coolify автоматически использует healthcheck из Dockerfile/docker-compose.
Убедитесь, что endpoint `/api/health` доступен.

### Resource Limits (опционально):

- **Memory**: 512MB (минимум)
- **CPU**: 0.5 cores

## 🔍 Проверка развертывания

После развертывания проверьте:

1. **Health Check**: `https://your-app.coolify.io/api/health`
   - Должен вернуть JSON с статусом

2. **Главная страница**: `https://your-app.coolify.io/`
   - Должен отображаться дашборд

3. **Логи**: В Coolify перейдите в Logs
   - Должно быть сообщение: `✅ MongoDB подключена успешно`

## 🐛 Решение проблем

### MongoDB не подключается

1. Проверьте имя сервиса MongoDB в Coolify
2. Убедитесь, что оба сервиса в одной сети
3. Проверьте переменную `MONGODB_URI`
4. Посмотрите логи MongoDB сервиса

### Health Check не проходит

1. Проверьте, что порт указан правильно
2. Убедитесь, что приложение запустилось (проверьте логи)
3. Health check может показывать "DEGRADED" если MongoDB не подключена - это нормально

### Приложение не запускается

1. Проверьте логи в Coolify
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что Dockerfile корректен

## 📝 Особенности Coolify

### Автоматические функции:

- ✅ Автоматическое определение Dockerfile
- ✅ Автоматическое управление сетями
- ✅ Автоматические health checks
- ✅ Автоматические перезапуски при сбоях
- ✅ SSL сертификаты (Let's Encrypt)

### Рекомендации:

1. **Используйте Git для развертывания** - Coolify автоматически обновит приложение при push
2. **Настройте webhooks** для автоматического деплоя
3. **Используйте отдельный сервис для MongoDB** - это упростит управление и масштабирование
4. **Настройте резервное копирование** для MongoDB через Coolify

## 🔄 CI/CD с Coolify

Coolify поддерживает автоматическое развертывание при push в Git:

1. Подключите репозиторий в Coolify
2. Настройте автоматический деплой на ветку `main` или `master`
3. При каждом push Coolify автоматически:
   - Соберет новый образ
   - Остановит старое приложение
   - Запустит новое приложение
   - Проверит health check

## 📚 Дополнительные ресурсы

- [Coolify Documentation](https://coolify.io/docs)
- [MongoDB Connection Strings](https://www.mongodb.com/docs/manual/reference/connection-string/)

---

**Готово!** Ваше приложение должно работать в Coolify 🎉

