# 🍃 Настройка MongoDB в Coolify

**⚠️ ВАЖНО:** MongoDB должна быть развернута как **ОТДЕЛЬНЫЙ сервис** в Coolify!

MongoDB **НЕ включается** в docker-compose.yaml или Nixpacks сборку. Это отдельный сервис.

## 🚀 Быстрая настройка MongoDB в Coolify

**⚠️ ВАЖНО: MongoDB развертывается ОТДЕЛЬНО от вашего приложения!**

### Вариант 1: MongoDB через Database (самый простой) ⭐

1. **В Coolify создайте новый ресурс:**
   - Нажмите "Create New Resource" или "New"
   - Выберите категорию **"Database"**
   - Выберите **"MongoDB"**

2. **Настройки:**
   - **Имя сервиса:** `mongodb` ⚠️ **ОБЯЗАТЕЛЬНО запомните это имя!**
   - **Версия:** `7` или оставьте по умолчанию
   - **Port:** `27017` (обычно устанавливается автоматически)

3. **Environment Variables (опционально):**
   ```env
   MONGO_INITDB_DATABASE=helloworld
   ```

4. **Нажмите Deploy** и дождитесь запуска

5. **Проверьте статус:** MongoDB должна быть в статусе "Running"

### Вариант 2: MongoDB как Docker Image

1. **Создайте новый ресурс в Coolify:**
   - Нажмите "Create New Resource"
   - Выберите тип: **"Docker Image"**

2. **Настройки:**
   - **Image:** `mongo:7`
   - **Название сервиса:** `mongodb` ⚠️ **ОБЯЗАТЕЛЬНО запомните это имя!**
   - **Port:** `27017`
   - **Environment Variables:**
     ```env
     MONGO_INITDB_DATABASE=helloworld
     ```

3. **Нажмите Deploy**

### Вариант 3: MongoDB через Docker Compose

Если нужен больший контроль:

1. Создайте новый ресурс → **"Docker Compose"**
2. Используйте следующий `docker-compose.yml`:
   ```yaml
   version: '3.8'
   services:
     mongodb:
       image: mongo:7
       restart: unless-stopped
       ports:
         - "27017:27017"
       environment:
         - MONGO_INITDB_DATABASE=helloworld
       volumes:
         - mongodb_data:/data/db
   volumes:
     mongodb_data:
   ```

### ⚠️ КРИТИЧЕСКИ ВАЖНО: Запомните имя сервиса!

**Имя сервиса MongoDB** - это то, что вы указали при создании (например, `mongodb`, `mongo`, `db`).

**Как узнать имя сервиса:**
1. Откройте ваш MongoDB сервис в Coolify
2. Посмотрите на **URL страницы** или **название ресурса** вверху
3. Обычно это имя, которое вы указали при создании

**Это имя понадобится для `MONGODB_URI` в вашем приложении!**

### Вариант 2: MongoDB Atlas (облачный)

Если используете MongoDB Atlas (облачный сервис):

1. Создайте кластер в [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)
2. Получите Connection String
3. Используйте его в переменной `MONGODB_URI`:
   ```
   mongodb+srv://user:password@cluster.mongodb.net/helloworld?retryWrites=true&w=majority
   ```

## 🔗 Настройка подключения приложения к MongoDB

После развертывания MongoDB, настройте приложение:

### Для Nixpacks или Docker Compose:

1. **В настройках вашего приложения** (не MongoDB!) добавьте переменную окружения:
   ```env
   MONGODB_URI=mongodb://mongodb:27017/helloworld
   ```
   
   ⚠️ **Важно:** 
   - Замените `mongodb` на **реальное имя вашего MongoDB сервиса** в Coolify
   - Если MongoDB в другой сети, может потребоваться полный hostname
   - В Coolify сервисы в одной сети могут обращаться друг к другу по имени

2. **Проверьте сеть:**
   - Убедитесь, что оба сервиса (приложение и MongoDB) в одной сети
   - Coolify обычно создает общую сеть автоматически

### Определение имени MongoDB сервиса:

1. В Coolify откройте ваш MongoDB сервис
2. Посмотрите на **URL или имя сервиса**
3. Обычно это имя, которое вы указали при создании (например, `mongodb`, `mongo`, `db`)

### Примеры MONGODB_URI:

**Локальный сервис в Coolify:**
```env
MONGODB_URI=mongodb://mongodb:27017/helloworld
```

**С аутентификацией:**
```env
MONGODB_URI=mongodb://username:password@mongodb:27017/helloworld?authSource=admin
```

**MongoDB Atlas:**
```env
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/helloworld?retryWrites=true&w=majority
```

**Внешний сервер:**
```env
MONGODB_URI=mongodb://mongodb.example.com:27017/helloworld
```

## ✅ Проверка подключения

После настройки:

1. **Перезапустите приложение** в Coolify
2. **Проверьте логи приложения:**
   ```
   ✅ MongoDB подключена успешно
   ```
3. **Проверьте Health Check:**
   - Откройте `https://your-app.coolify.io/api/health`
   - Должен показать `"database": "ok"`

4. **Проверьте дашборд:**
   - Откройте `https://your-app.coolify.io/`
   - В секции MongoDB должен быть статус "Подключено"

## 🐛 Решение проблем

### Проблема: "MongoDB не подключается"

**Решения:**

1. **Проверьте имя сервиса:**
   - Убедитесь, что имя в `MONGODB_URI` совпадает с именем сервиса MongoDB
   - В Coolify имя сервиса видно в настройках ресурса

2. **Проверьте сеть:**
   - Оба сервиса должны быть в одной сети
   - В Coolify это обычно происходит автоматически

3. **Проверьте порт:**
   - MongoDB должна слушать на порту `27017`
   - Проверьте настройки порта в MongoDB сервисе

4. **Проверьте логи MongoDB:**
   - В Coolify откройте логи MongoDB сервиса
   - Должны быть сообщения о запуске

5. **Проверьте переменные окружения:**
   - Убедитесь, что `MONGODB_URI` установлена в **приложении**, а не в MongoDB
   - Перезапустите приложение после изменения переменных

### Проблема: "Connection refused"

**Решения:**

1. MongoDB может еще запускаться - подождите 30-60 секунд
2. Проверьте, что MongoDB сервис запущен (статус "Running")
3. Попробуйте использовать IP адрес вместо имени сервиса (временно для теста)

### Проблема: "Name resolution failed"

**Решения:**

1. Убедитесь, что оба сервиса в одном проекте/сети
2. Попробуйте использовать полный hostname (если доступен в Coolify)
3. Проверьте DNS настройки в Coolify

## 📝 Резюме

1. ✅ MongoDB = **отдельный сервис** в Coolify
2. ✅ Создайте MongoDB сервис **ДО** развертывания приложения
3. ✅ Запомните **имя сервиса** MongoDB
4. ✅ Установите `MONGODB_URI` в **приложении** (не в MongoDB)
5. ✅ Используйте формат: `mongodb://<service-name>:27017/helloworld`

---

**Готово!** Теперь MongoDB должна работать с вашим приложением 🎉
